
class Polynomial {
	add(s1, s2){
	}

	sub(s1, s2){
	}

	mul(s1, s2){
	}

	mod(fx, gx){
		let modulo = []
		let modtmp = fx
		for(let i = 0; i < gx.length; ++i) modtmp.push(0)
		console.log(modtmp)

		for(let i = 0; i < gx.length; ++i){

		}

		return modulo
	}
}
